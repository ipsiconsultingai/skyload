# 기능명

> 한 줄 요약: [이 기능이 무엇인지 한 문장으로]

## 개요

### 배경

[왜 이 기능이 필요한가? 어떤 문제를 해결하는가?]

### 목표

[이 기능을 통해 달성하고자 하는 것]

## 사용자 시나리오

### 시나리오 1: [시나리오명]

```
1. 사용자가 [행동]
2. 시스템이 [반응]
3. 사용자가 [결과 확인]
```

### 시나리오 2: [시나리오명] (선택)

```
1. ...
```

## 요구사항

### 기능 요구사항

우선순위: 🔴 필수 | 🟡 권장 | 🟢 선택

- [ ] 🔴 요구사항 1
- [ ] 🔴 요구사항 2
- [ ] 🟡 요구사항 3
- [ ] 🟢 요구사항 4

### 비기능 요구사항

- **성능**: [구체적 수치 - 예: 응답 시간 200ms 이내]
- **보안**: [구체적 요구사항 - 예: 인증 필요, CSRF 토큰 검증]
- **접근성**: [구체적 요구사항 - 예: 키보드 네비게이션 지원]

## 구현 가이드

### 구현 위치

```
이 기능을 구현할 위치를 명시합니다.

신규 모듈:
└── libs/새모듈명/
    ├── core.ts
    ├── types.ts
    └── index.ts

또는

기존 모듈 확장:
└── libs/기존모듈명/
    └── 새파일.ts (추가)

또는

페이지/컴포넌트:
└── app/경로/
    └── page.tsx
```

### 기존 코드 활용

이 기능 구현에 활용할 기존 모듈:

| 모듈                  | 용도     | 참고                     |
| --------------------- | -------- | ------------------------ |
| `libs/fetch`          | API 호출 | getFetch, postFetch 사용 |
| `libs/services/oauth` | 인증     | createOAuthClient 활용   |

### 신규 의존성 (해당시)

```bash
pnpm add 패키지명    # 용도 설명
```

## 상세 스펙

### 타입 정의

```typescript
// 핵심 타입 정의
interface FeatureName {
  id: string;
  // ...
}

// 요청/응답 타입
interface CreateFeatureRequest {
  // ...
}

interface CreateFeatureResponse {
  // ...
}
```

### API 설계 (해당시)

| 엔드포인트          | 메서드 | 설명      | 인증 | 요청              | 응답         |
| ------------------- | ------ | --------- | ---- | ----------------- | ------------ |
| `/api/resource`     | GET    | 목록 조회 | 필요 | `{ page, limit }` | `Resource[]` |
| `/api/resource`     | POST   | 생성      | 필요 | `CreateRequest`   | `Resource`   |
| `/api/resource/:id` | DELETE | 삭제      | 필요 | -                 | `void`       |

#### 요청/응답 예시

**GET /api/resource**

```json
// Request
{
  "page": 1,
  "limit": 10
}

// Response (성공)
{
  "data": [...],
  "total": 100,
  "page": 1
}

// Response (에러)
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "로그인이 필요합니다."
  }
}
```

### 컴포넌트 설계 (해당시)

```
ComponentName/
├── ComponentName.tsx      # 메인 컴포넌트
├── ComponentName.test.tsx # 테스트
├── useComponentName.ts    # 커스텀 훅 (필요시)
└── types.ts               # 컴포넌트 전용 타입
```

#### Props 인터페이스

```typescript
interface ComponentNameProps {
  // 필수 props
  data: DataType;
  onAction: (id: string) => void;

  // 선택 props
  className?: string;
  disabled?: boolean;
}
```

#### 상태 관리

- **서버/클라이언트**: [서버 컴포넌트 | 클라이언트 컴포넌트 | 혼합]
- **로컬 상태**: [useState로 관리할 상태 목록]
- **전역 상태**: [필요시 - Context, Zustand 등]
- **서버 상태**: [필요시 - React Query, SWR 등]

### UI/UX (해당시)

#### 화면 구성

```
┌─────────────────────────────────┐
│  Header                         │
├─────────────────────────────────┤
│                                 │
│  Main Content                   │
│                                 │
├─────────────────────────────────┤
│  Footer                         │
└─────────────────────────────────┘
```

#### 인터랙션

- **로딩**: [스켈레톤 | 스피너 | 없음]
- **에러**: [토스트 | 인라인 메시지 | 에러 페이지]
- **성공**: [토스트 | 리다이렉트 | UI 업데이트]

## 에러 처리

### 예상 에러 케이스

| 에러 상황     | 에러 코드          | 사용자 메시지                   | 처리 방법          |
| ------------- | ------------------ | ------------------------------- | ------------------ |
| 네트워크 오류 | `NETWORK_ERROR`    | "네트워크 연결을 확인해주세요." | 재시도 버튼 표시   |
| 인증 만료     | `UNAUTHORIZED`     | "다시 로그인해주세요."          | 로그인 페이지 이동 |
| 권한 없음     | `FORBIDDEN`        | "접근 권한이 없습니다."         | 이전 페이지 이동   |
| 입력값 오류   | `VALIDATION_ERROR` | [필드별 메시지]                 | 인라인 에러 표시   |

### 에러 복구 전략

- **자동 재시도**: [해당시 - 몇 회, 어떤 조건에서]
- **폴백**: [해당시 - 캐시 데이터 표시 등]
- **사용자 액션**: [재시도 버튼, 새로고침 안내 등]

## 성공 기준

### 완료 조건

- [ ] 모든 기능 요구사항(🔴) 구현
- [ ] 타입 체크 통과 (`pnpm build`)
- [ ] 린트 통과 (`pnpm lint`)
- [ ] 에러 케이스 처리 완료

### 검증 방법

```bash
# 1. 타입 체크
pnpm build

# 2. 린트
pnpm lint

# 3. 수동 테스트 (해당시)
# - 시나리오 1 수행
# - 시나리오 2 수행
```

> **참고**: 테스트 코드는 `/create-jest`, 문서화는 `/w-context` 명령어로 별도 진행합니다.

## 구현 단계 (복잡한 기능의 경우)

### Phase 1: MVP

- [ ] 핵심 기능 1
- [ ] 핵심 기능 2

### Phase 2: 개선

- [ ] 추가 기능 1
- [ ] 추가 기능 2

### Phase 3: 고도화 (선택)

- [ ] 최적화
- [ ] 추가 기능

## 제약사항

- [기술적 제약 - 예: IE11 미지원]
- [비즈니스 제약 - 예: 외부 API 호출 제한]
- [시간적 제약 - 예: Phase 1만 우선 구현]

## 참고자료

- [디자인 시안](링크)
- [API 문서](링크)
- [관련 이슈](링크)

---

## 작성 가이드

<details>
<summary>이 템플릿 사용법 (작성 후 삭제)</summary>

### 파일 네이밍 규칙

```
{순번}-{기능명}.md
```

| 항목   | 규칙                      | 예시                     |
| ------ | ------------------------- | ------------------------ |
| 순번   | 3자리 숫자                | `001`, `002`, `010`      |
| 구분자 | 하이픈                    | `-`                      |
| 기능명 | kebab-case, 영문, 2~3단어 | `user-auth`, `link-crud` |

**예시:**

```
specs/
├── _template.md
├── 001-user-auth.md
├── 002-link-crud.md
├── 003-folder-tree.md
└── 010-search.md
```

### 필수 섹션

- 개요 (배경, 목표)
- 사용자 시나리오 (최소 1개)
- 기능 요구사항 (우선순위 표시)
- 구현 위치
- 성공 기준

### 선택 섹션 (해당시 작성)

- API 설계: 백엔드 API가 필요한 경우
- 컴포넌트 설계: UI 컴포넌트가 필요한 경우
- 신규 의존성: 새 패키지가 필요한 경우
- 구현 단계: 복잡한 기능의 경우

### 작성 팁

1. **구체적으로**: "빠르게" 대신 "200ms 이내"
2. **예시 포함**: 요청/응답 예시, 시나리오 예시
3. **에러 케이스**: 정상 케이스만큼 중요
4. **우선순위**: 🔴🟡🟢로 명확히 구분

### 작업 흐름

```bash
/spec {명세서}     # 1. 기능 구현
/create-jest       # 2. 테스트 작성
/w-context         # 3. 문서화
/pr                # 4. PR 생성
```

</details>
